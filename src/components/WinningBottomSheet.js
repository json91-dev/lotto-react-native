import React from 'react';
import {
  StyleSheet,
  View,
} from 'react-native';

const WinningBottomSheet = () =>{
  return (
    <View style={styles.container} />
  );
};

export default WinningBottomSheet;

const styles = StyleSheet.create({
  container: {
    backgroundColor: 'blue',
    borderWidth: 10,
    height: '100%',
    borderColor: 'pink'
  },
});
